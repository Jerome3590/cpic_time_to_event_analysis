# Step 7: SHAP Analysis

SHAP value computation and analysis for falls and ED models.

## TODO
- [ ] Copy `7_shap_analysis/` scripts from `pgx-analysis` as baseline
- [ ] Generate SHAP values for falls models (per bin, per age band)
- [ ] Generate SHAP values for ED models (per bin, per age band)
- [ ] Compare SHAP profiles: which features differ between falls vs. ED risk?
- [ ] Identify top SHAP features per age band for falls outcome
